{% extends 'base.html.twig' %}

{% block body %}
<div class="container panel panel-default">
    <h1>Tagsvocabulary</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tagsVocabulary.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tagsVocabulary.name }}</td>
            </tr>
            <tr>
                <th>Entitytypeid</th>
                <td>{{ tagsVocabulary.entityTypeId }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tagsvocabulary_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tagsvocabulary_edit', { 'id': tagsVocabulary.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
</div>
{% endblock %}
